export const MAX_COLORS = 12;

// Output caps (anti-asset-lifting guardrails)
export const OUTPUT_CAPS = {
  maxTileLongSidePx: 3000,
  maxCanvasPixels: 40000000,   // 40 MP
  maxExportLongEdgePx: 4096,
  maxExportDpi: 300,
};
